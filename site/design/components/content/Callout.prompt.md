A note, caveat or warning inside docs prose.

```jsx
<Callout tone="info" label="note">Auth is inherited from your own <CodeChip>claude</CodeChip> CLI credentials.</Callout>
```

8% tint of the signal color, 28–30% border. One per idea; three in a row means the prose is doing the wrong job.
