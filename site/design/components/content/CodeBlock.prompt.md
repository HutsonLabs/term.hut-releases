Any shell transcript or source sample.

```jsx
<CodeBlock language="zsh">
  <div><span className="thut-code__prompt">$</span> hut open ~/src/term.hut</div>
  <div className="hl-cmt"># anchors the repo, attaches its session</div>
</CodeBlock>
```

Prompt sigil in peach. Syntax colors come from the `--hl-*` tokens / `.hl-*` classes — the same class names the app's chat panel emits, so a snippet copied out of the app lands already styled. On mobile the block scrolls rather than wraps.
