One release block on /changelog.

```jsx
<ChangelogEntry version="0.9.4" date="2026-08-02" label="added" asset="term.hut_0.9.4_aarch64.dmg · 5.4 MB"
  items={[<><CodeChip>/btw</CodeChip> asks a side question without costing the turn.</>]} />
```

Version in peach mono at 18px. Entries come from the releases feed — the site never types them in by hand.
