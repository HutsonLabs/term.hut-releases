Docs search and the few forms the site has.

```jsx
<Input placeholder="Search the docs" />
<Input value="not-a-version" invalid />
```

44px tall, crust fill, overlay0 border. Placeholder is `--ink-dim` and never a fake value.
