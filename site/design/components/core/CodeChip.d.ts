/** CodeChip — inline code, path, flag or slash command inside running text. */
export interface CodeChipProps { children?: React.ReactNode }
export function CodeChip(props: CodeChipProps): JSX.Element;
