The 11px mono uppercase label that titles a card or a spec block.

```jsx
<Eyebrow>Buttons</Eyebrow>
```

Uses `--ink-dim` (overlay2, 5.88:1) — never overlay1, which fails AA at this size. It is a label, not a heading: keep it out of the h1–h6 outline.
