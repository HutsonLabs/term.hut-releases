Wraps every app screenshot on the site.

```jsx
<ScreenshotFrame title="term.hut — ~/src/term.hut" src="/img/split.avif"
  alt="term.hut split view: a shell on the left, the Claude panel on the right mid-turn, both anchored to the same repo." />
```

16px radius, mantle titlebar, crust body. With no `src` it shows a hatched placeholder — the product has no shipped screenshots yet, so that is the honest state. Alt text describes what the window is doing.
