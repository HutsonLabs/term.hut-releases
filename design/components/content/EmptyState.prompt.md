The empty changelog, a failed release fetch, an unavailable download.

```jsx
<EmptyState title="No releases yet.">The feed reads the releases repo directly, so this page fills itself the moment a dmg is published.</EmptyState>
```

Dashed overlay0 border on a crust ground. Always say why it is empty; never apologise and never promise a date.
