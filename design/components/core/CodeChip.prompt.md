Inline code inside prose — a path, a flag, a slash command.

```jsx
It runs behind a FIFO in <CodeChip>~/.term-hut/agents/&lt;id&gt;/</CodeChip>.
```

5px radius, `--code-chip-bg` wash, `--ctp-peach` text. Do not use it for multi-line code — that is CodeBlock.
