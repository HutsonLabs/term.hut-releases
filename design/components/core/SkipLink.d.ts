/** SkipLink — hidden until focused, then a peach pill at top left. */
export interface SkipLinkProps { href?: string; children?: React.ReactNode }
export function SkipLink(props: SkipLinkProps): JSX.Element;
