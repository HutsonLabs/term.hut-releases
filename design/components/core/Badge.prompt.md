A mono pill for version numbers, requirements and capabilities.

```jsx
<Badge>v0.9.4</Badge>
<Badge tone="ok">signed &amp; notarized</Badge>
<Badge tone="remote">ssh</Badge>
<Badge tone="warn">macOS 12+</Badge>
```

Tones map to the palette's signal colors exactly as the app uses them. Never put marketing language inside one.
