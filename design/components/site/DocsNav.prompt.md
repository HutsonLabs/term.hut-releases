The docs sidebar, 200px wide.

```jsx
<DocsNav current="detached-agents" groups={[{title:'Concepts',items:[{slug:'anchored-workspaces',label:'Anchored workspaces'}]}]} />
```

Slugs are the markdown filenames. Active row is the one place mauve appears outside focus. Below 1024 this becomes a sticky select above the prose.
