The three positioning claims on the home page, and one per section on /features.

```jsx
<FeatureCard number="01" title="Detached agents" href="/docs/detached-agents">
  Claude sessions aren't child processes. Closing the app detaches; reopening re-attaches and replays the stream.
</FeatureCard>
```

Carries gradient treatment C: a 1px greys-only border running overlay0 → surface0. State the mechanism, not the benefit.
